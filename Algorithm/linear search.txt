#include <stdio.h>
int main()
{
  int array[100],search,i,n;
  printf("number of elements\n");
  scanf("%d",&n);
  printf("your number of elements\n");
  for (i=0;i<n;i++){
    scanf("%d", &array[i]);
 }
  printf("needed number to search\n");
  scanf("%d",&search);
  for (i=0;i<n;i++)
  {
    if (array[i]==search)
    {
            printf("%d found at location %d.\n",search,i+1);
            break;
    }
  }
  if(i==n)
    printf("%d isn't present in the array.\n",search);
  return 0;
}
